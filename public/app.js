const presets=["Mortgage / Rent","Electric","Heating / Gas","Water / Sewer","Trash","Internet","Cell phone","Car payment","Car insurance","Gas / Fuel","Health insurance","Prescription / Medical","Groceries","Credit card","Personal loan","Student loan","Childcare","Pet care","Netflix","Hulu","Disney+","Amazon Prime","Spotify / Music","Gym membership","Cloud storage","Property tax","Vehicle tax / Registration","Home insurance","Life insurance","Storage unit"];
let state={balance:0,weeklyIncome:0,expenses:[],accounts:[],plannedExpenses:[]},openWeek=null,editingId=null,inlineEditingId=null;
const $=s=>document.querySelector(s),money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n),esc=s=>String(s).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
function startOfWeek(d){const x=new Date(d),diff=(x.getDay()+6)%7;x.setDate(x.getDate()-diff);x.setHours(0,0,0,0);return x}function endOfWeek(d){const x=new Date(d);x.setDate(x.getDate()+6);return x}function fmt(d){return d.toLocaleDateString("en-US",{month:"short",day:"numeric"})}function dateFmt(s){if(!s)return "No date";return new Date(s+"T12:00:00").toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"})}
function occ(start,end){const out=[];for(const e of state.expenses){if(e.frequency==="weekly"){out.push({...e,date:new Date(start)});continue}let c=new Date(start.getFullYear(),start.getMonth(),1),final=new Date(end.getFullYear(),end.getMonth(),1);while(c<=final){const last=new Date(c.getFullYear(),c.getMonth()+1,0).getDate(),d=new Date(c.getFullYear(),c.getMonth(),Math.min(e.dueDay,last));if(d>=start&&d<=end)out.push({...e,date:d});c=new Date(c.getFullYear(),c.getMonth()+1,1)}}return out.sort((a,b)=>a.date-b.date)}
async function save(){const r=await fetch("/api/state",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(state)});if(!r.ok)throw new Error();state=await r.json()}async function load(){const r=await fetch("/api/state");state=await r.json();state.accounts??=[];state.plannedExpenses??=[];render()}
function render(){if(document.activeElement!==$("#balanceInput"))$("#balanceInput").value=Number(state.balance).toFixed(2);if(document.activeElement!==$("#incomeInput"))$("#incomeInput").value=Number(state.weeklyIncome).toFixed(2);renderForecast();renderAccounts();renderPlanned();renderExpenses();renderPresets()}
function renderForecast(){const box=$("#forecast");box.innerHTML="";let running=state.balance;const first=startOfWeek(new Date());for(let i=0;i<6;i++){const ws=new Date(first);ws.setDate(ws.getDate()+7*i);const we=endOfWeek(ws),items=occ(ws,we),bills=items.reduce((s,x)=>s+x.amount,0),start=running,end=start+state.weeklyIncome-bills;running=end;const el=document.createElement("div");el.className="week";el.innerHTML=`<div class="weeksum"><div class="weekdate"><strong>${i===0?"This week":i===1?"Next week":"Week "+(i+1)}</strong><div class="small">${fmt(ws)} – ${fmt(we)}</div></div><div><div class="small">Income</div><div class="amt">+${money(state.weeklyIncome)}</div></div><div><div class="small">Expenses</div><div class="amt">−${money(bills)}</div></div><div><div class="small">Expected balance</div><div class="amt ${end<0?"bad":"good"}">${money(end)}</div></div><button class="btn weekaction" data-week="${i}">${openWeek===i?"Hide details":"See this week"}</button></div>`;if(openWeek===i){const d=document.createElement("div");d.className="details";d.innerHTML=`<div class="line"><span class="label">Starting available</span><strong>${money(start)}</strong></div><div class="line"><span>Weekly income</span><strong>+${money(state.weeklyIncome)}</strong></div>`+(items.length?items.map(x=>`<div class="line"><span>${esc(x.name)} <span class="small">${fmt(x.date)}</span></span><strong>−${money(x.amount)}</strong></div>`).join(""):`<div class="line"><span class="label">No expenses due this week</span><span>—</span></div>`)+`<div class="line total"><span>Expected available</span><span class="${end<0?"bad":"good"}">${money(end)}</span></div>`;el.appendChild(d)}box.appendChild(el)}box.querySelectorAll("[data-week]").forEach(b=>b.onclick=()=>{const i=Number(b.dataset.week);openWeek=openWeek===i?null:i;renderForecast()})}
function renderAccounts(){const box=$("#accounts"),total=state.accounts.reduce((s,a)=>s+a.balance,0);$("#accountSummary").innerHTML=`<div><div class="small">Available</div><div class="summary-number">${money(state.balance)}</div></div><div><div class="small">Set aside</div><div class="summary-number">${money(total)}</div></div><div><div class="small">Total money</div><div class="summary-number">${money(state.balance+total)}</div></div>`;box.innerHTML=state.accounts.length?"":"<div class=\"empty\">Nothing set aside yet. That is completely fine.</div>";state.accounts.forEach(a=>{const row=document.createElement("div");row.className="account-row";row.innerHTML=`<div><strong>${esc(a.name)}</strong><div class="small">Set aside</div></div><strong>${money(a.balance)}</strong><div class="actions"><button class="btn" data-return="${a.id}">Move back</button><button class="btn danger" data-delete-account="${a.id}">Remove</button></div>`;box.appendChild(row)});box.querySelectorAll("[data-return]").forEach(b=>b.onclick=async()=>{const a=state.accounts.find(x=>x.id===Number(b.dataset.return));const raw=prompt(`How much should move from ${a.name} back to available?`,a.balance.toFixed(2));if(raw===null)return;const v=Number(raw);if(!Number.isFinite(v)||v<=0||v>a.balance)return;a.balance-=v;state.balance+=v;if(a.balance===0)state.accounts=state.accounts.filter(x=>x.id!==a.id);await save();render()});box.querySelectorAll("[data-delete-account]").forEach(b=>b.onclick=async()=>{const a=state.accounts.find(x=>x.id===Number(b.dataset.deleteAccount));state.balance+=a.balance;state.accounts=state.accounts.filter(x=>x.id!==a.id);await save();render()})}
function renderPlanned(){const box=$("#plannedExpenses");box.innerHTML=state.plannedExpenses.length?"":"<div class=\"empty\">No future expenses added yet.</div>";[...state.plannedExpenses].sort((a,b)=>a.targetDate.localeCompare(b.targetDate)).forEach(p=>{const target=new Date(p.targetDate+"T12:00:00"),today=new Date(),days=Math.max(1,Math.ceil((target-today)/86400000)),weeks=Math.max(1,Math.ceil(days/7)),remaining=Math.max(0,p.amount-p.saved),weekly=remaining/weeks,pct=p.amount?Math.min(100,p.saved/p.amount*100):0,row=document.createElement("div");row.className="planned-row";row.innerHTML=`<div><strong>${esc(p.name)}</strong><div class="small meta">${dateFmt(p.targetDate)}${p.repeatsYearly?" · yearly":""}</div><div class="progress"><span style="width:${pct}%"></span></div></div><strong>${money(p.amount)}</strong><div><div class="small">To stay on track</div><div class="target">${remaining<=0?"Ready":money(weekly)+" / week"}</div><div class="small">${money(p.saved)} already set aside</div></div><div class="actions"><button class="btn" data-fund="${p.id}">Set aside</button><button class="btn danger" data-delete-plan="${p.id}">Remove</button></div>`;box.appendChild(row)});box.querySelectorAll("[data-fund]").forEach(b=>b.onclick=async()=>{const p=state.plannedExpenses.find(x=>x.id===Number(b.dataset.fund)),raw=prompt(`How much do you want to set aside for ${p.name}?`);if(raw===null)return;const v=Number(raw);if(!Number.isFinite(v)||v<=0||v>state.balance)return;p.saved+=v;state.balance-=v;await save();render()});box.querySelectorAll("[data-delete-plan]").forEach(b=>b.onclick=async()=>{const p=state.plannedExpenses.find(x=>x.id===Number(b.dataset.deletePlan));state.balance+=p.saved;state.plannedExpenses=state.plannedExpenses.filter(x=>x.id!==p.id);await save();render()})}
function renderExpenses(){const box=$("#expenses");box.innerHTML="";[...state.expenses].sort((a,b)=>a.dueDay-b.dueDay).forEach(e=>{const row=document.createElement("div");row.className="expense"+(inlineEditingId===e.id?" editing":"");row.innerHTML=`<div><strong>${esc(e.name)}</strong><div class="small meta">${e.frequency==="weekly"?"Every week":"Due on the "+e.dueDay}</div></div><strong>${money(e.amount)}</strong><span class="label">${e.frequency}</span><div class="actions"><button class="btn primary" data-edit="${e.id}">${inlineEditingId===e.id?"Close":"Edit"}</button><button class="btn danger" data-remove="${e.id}">Remove</button></div>`;if(inlineEditingId===e.id){const edit=document.createElement("div");edit.className="inline-edit";edit.innerHTML=`<div class="formgrid"><div class="field"><label>Expense name</label><input data-inline-name value="${esc(e.name)}"></div><div class="field"><label>Amount</label><input data-inline-amount type="number" min="0" step=".01" value="${e.amount}"></div><div class="field"><label>How often</label><select data-inline-frequency><option value="monthly" ${e.frequency==="monthly"?"selected":""}>Monthly</option><option value="weekly" ${e.frequency==="weekly"?"selected":""}>Weekly</option></select></div><div class="field"><label>Due day</label><input data-inline-day type="number" min="1" max="31" value="${e.dueDay}"></div></div><div class="formactions"><button class="btn primary" type="button" data-save-expense="${e.id}">Save changes</button><button class="btn" type="button" data-cancel-expense>Cancel</button></div><div class="status" data-inline-status></div>`;row.appendChild(edit)}box.appendChild(row)});box.querySelectorAll("[data-edit]").forEach(b=>b.onclick=()=>{const id=Number(b.dataset.edit);inlineEditingId=inlineEditingId===id?null:id;renderExpenses()});box.querySelectorAll("[data-remove]").forEach(b=>b.onclick=async()=>{state.expenses=state.expenses.filter(x=>x.id!==Number(b.dataset.remove));await save();render()});box.querySelectorAll("[data-cancel-expense]").forEach(b=>b.onclick=()=>{inlineEditingId=null;renderExpenses()});box.querySelectorAll("[data-save-expense]").forEach(b=>b.onclick=async()=>{const row=b.closest(".expense"),id=Number(b.dataset.saveExpense),name=row.querySelector("[data-inline-name]").value.trim(),amount=Number(row.querySelector("[data-inline-amount]").value),frequency=row.querySelector("[data-inline-frequency]").value==="weekly"?"weekly":"monthly",day=Number(row.querySelector("[data-inline-day]").value),status=row.querySelector("[data-inline-status]");if(!name||!Number.isFinite(amount)||amount<0||!Number.isInteger(day)||day<1||day>31){status.textContent="Enter a valid name, amount, and due day.";return}Object.assign(state.expenses.find(x=>x.id===id),{name,amount,frequency,dueDay:day});await save();inlineEditingId=null;render()})}
function renderPresets(){const grid=$("#presetGrid");grid.innerHTML="";const existing=new Set(state.expenses.map(e=>e.name));for(const name of presets){const b=document.createElement("button");b.className="preset";b.type="button";b.textContent=name+(existing.has(name)?" ✓":"");b.disabled=existing.has(name);if(!b.disabled)b.onclick=()=>openExpense(name);grid.appendChild(b)}}
function openExpense(name=""){$("#formTitle").textContent=name?"Add "+name:"Custom expense";$("#formHelp").textContent="Enter the amount and when it normally happens.";$("#formFields").innerHTML=`<div class="field"><label>Name</label><input name="name" value="${esc(name)}"></div><div class="field"><label>Amount</label><input name="amount" type="number" min="0" step=".01"></div><div class="field"><label>How often</label><select name="frequency"><option value="monthly">Monthly</option><option value="weekly">Weekly</option></select></div><div class="field"><label>Due day</label><input name="dueDay" type="number" min="1" max="31" value="1"></div>`;$("#presetsPanel").classList.remove("open");$("#formPanel").classList.add("open");$("#formPanel").scrollIntoView({behavior:"smooth",block:"nearest"})}
async function saveQuick(kind){const input=kind==="balance"?$("#balanceInput"):$("#incomeInput"),note=kind==="balance"?$("#balanceNote"):$("#incomeNote"),value=Number(input.value);if(!Number.isFinite(value)||value<0){note.textContent="Enter a valid amount.";return}if(kind==="balance")state.balance=value;else state.weeklyIncome=value;await save();note.textContent="Saved.";render()}
$("#saveBalance").onclick=()=>saveQuick("balance");$("#saveIncome").onclick=()=>saveQuick("income");$("#balanceInput").onkeydown=e=>{if(e.key==="Enter")saveQuick("balance")};$("#incomeInput").onkeydown=e=>{if(e.key==="Enter")saveQuick("income")};
$("#addAccount").onclick=()=>{$("#accountFormPanel").classList.add("open");$("#accountFormPanel").scrollIntoView({behavior:"smooth"})};$("#cancelAccount").onclick=()=>$("#accountFormPanel").classList.remove("open");$("#accountForm").onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),name=String(fd.get("name")||"").trim(),amount=Number(fd.get("amount"));if(!name||!Number.isFinite(amount)||amount<=0||amount>state.balance){$("#accountStatus").textContent="Enter a name and an amount that is available.";return}state.balance-=amount;state.accounts.push({id:Date.now(),name,balance:amount});await save();e.currentTarget.reset();$("#accountFormPanel").classList.remove("open");render()};
$("#addPlanned").onclick=()=>{$("#plannedFormPanel").classList.add("open");$("#plannedFormPanel").scrollIntoView({behavior:"smooth"})};$("#cancelPlanned").onclick=()=>$("#plannedFormPanel").classList.remove("open");$("#plannedForm").onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),name=String(fd.get("name")||"").trim(),amount=Number(fd.get("amount")),targetDate=String(fd.get("targetDate")||""),saved=Number(fd.get("saved")||0);if(!name||!Number.isFinite(amount)||amount<=0||!targetDate||saved<0||saved>state.balance){$("#plannedStatus").textContent="Enter a name, amount, date, and a valid set-aside amount.";return}state.balance-=saved;state.plannedExpenses.push({id:Date.now(),name,amount,targetDate,saved,repeatsYearly:fd.get("repeatsYearly")==="on"});await save();e.currentTarget.reset();$("#plannedFormPanel").classList.remove("open");render()};
$("#showPresets").onclick=()=>{$("#formPanel").classList.remove("open");$("#presetsPanel").classList.add("open");$("#presetsPanel").scrollIntoView({behavior:"smooth"})};$("#closePresets").onclick=()=>$("#presetsPanel").classList.remove("open");$("#customExpense").onclick=()=>openExpense();$("#cancelForm").onclick=()=>$("#formPanel").classList.remove("open");$("#form").onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),name=String(fd.get("name")||"").trim(),amount=Number(fd.get("amount")),dueDay=Number(fd.get("dueDay")),frequency=fd.get("frequency")==="weekly"?"weekly":"monthly";if(!name||!Number.isFinite(amount)||amount<0||!Number.isInteger(dueDay)||dueDay<1||dueDay>31){$("#status").textContent="Enter a valid name, amount, and due day.";return}state.expenses.push({id:Date.now(),name,amount,dueDay,frequency});await save();e.currentTarget.reset();$("#formPanel").classList.remove("open");render()};
load();
